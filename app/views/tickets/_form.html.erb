<%= form_for(@ticket, :url => @ticket.new_record? ? project_tickets_path(@project) : project_ticket_path(@project, @ticket)) do |f| %>
  <% if @ticket.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@ticket.errors.count, "error") %> prohibited this ticket from being saved:</h2>

      <ul>
      <% @ticket.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :title %><br />
    <%= f.text_field :title %>
  </div>
  <div class="field">
    <%= f.label :status %><br />
    <%= f.text_field :status %>
  </div>
  <div class="field">
    <%= f.label :description %><br />
    <%= f.text_area :description %>
  </div>
  <div class="field">
    <%= f.label :priority %><br />
    <%= select(:ticket, :priority, @priorities) %>
  </div>
  <div class="field">
    <%= f.label :assigned_to_user_id %><br />
    <%= select(:ticket, :assigned_to_user_id, @users_array) %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
